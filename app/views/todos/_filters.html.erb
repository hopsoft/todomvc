<section class="footer">
  <span class="todo-count">
    <strong><%= @all_todos.active.count %></strong> <%= "item".pluralize @all_todos.active.count %> left
  </span>

  <ul class="filters">
    <li>
      <%= link_to "All", "#", class: token_list("selected": @filter == "all"),
        data: { reflex: "click->TodosReflex#filter", filter: "all" } %>
    </li>
    <li>
      <%= link_to "Active", "#", class: token_list("selected": @filter == "active"),
        data: { reflex: "click->TodosReflex#filter", filter: "active" } %>
    </li>
    <li>
      <%= link_to "Completed", "#", class: token_list("selected": @filter == "completed"),
        data: { reflex: "click->TodosReflex#filter", filter: "completed" } %>
    </li>
  </ul>

  <% if @all_todos.completed.any? %>
    <button class="clear-completed" data-reflex="click->TodosReflex#destroy_completed">
      Clear completed
    </button>
  <% end %>
</section>
